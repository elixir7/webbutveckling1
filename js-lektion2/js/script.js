












/*
var buyPrice = 5000;
var sellPrice = 6200;

var profitMargin = 1.25;

var answer = sellPrice > buyPrice * profitMargin

console.log("Janne will make money on the pryl: " + answer)
*/



/*
function windChillEffect(t, v){
	// t = air temperature (C)
	// v = wind speed (m/s)
	var vPow = Math.pow(v, 0.16);
    var feelsLike = 13.12 + 0.6215*t - 11.37*vPow + 0.3965*t*vPow;
    return 'Feels like ' + feelsLike
} 
*/

/*
function shouldJanneBuy(buyPrice, sellPrice, profitMargin){	
    var profitable = sellPrice > buyPrice * profitMargin;
    return profitable;
} 
*/

/*
var button = document.getElementById("button");
button.addEventListener("click", calcArea)

var outputField = document.getElementById("area")

function calcArea() {
    var height = document.getElementById("height").value;
    var base = document.getElementById("base").value;

    var area = (base * height) / 2;
    console.log(height)
    console.log(base)
    console.log(area)

    outputField.innerHTML = " " + area + " cm^2";

}
*/

